アルゴリズムとプログラミング

Javascript：動的型付け
TypeScript：静的型付け

Ajax：非同期通信で動的なWebアプリケーションを実現する、Googleマップとか検索エンジンの候補を表示するのに使われる
XSLT：XMLを他のXMLやHTMLに変換するスクリプト

リエントラント(再入可能)：別々に実行できる(=データ領域が異なる)
リユーザブル(再使用可能)：主記憶に1回ロードすれば実行できる、(＝変数を初期化してから実行する)
リカーシブ(再帰可能)：再帰的、すなわち再入可能
リロケータブル(再配置可能)：主記憶上のどの位置にいても実行できる

UTF-8/16：符号化しなくていい
妥当なXML文書←DTDに対応している

ヒープ領域：双方向リスト
サーブレット：Webサーバ上で動くプログラム、再入可能

JSON：index1: value1, index2: value2, ...で指定するデータ形式
抽象データ型：データ構造とそれの操作をカプセル化した型

オーバーロード：同じ名前のメソッド

Java←ガベージコレクションある